{
    "version": "0.0.1",
    "ptr_size": 2,
    "insts": {
        "KIL": {
            "args": 0,
            "description": "End process"
        },
        "OUT": {
            "args": 2,
            "description": "Print arg2 as arg1(type: 0(str), 1(char), 2(dec))"
        },
        "SET": {
            "args": 1,
            "description": "Set variable into memory. First arg is 1 byte of how many next bytes should be the value"
        },
        "SUM": {
            "args": 3,
            "description": "Sum two numbers and last arg is destination. Args are addreses" 
        },
        "SUB": {
            "args": 3,
            "description": "Substract two numbers and last arg is destination. Args are addreses" 
        }
    }
}