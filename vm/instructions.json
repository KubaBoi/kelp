{
    "version": "0.0.1",
    "ptr_size": 2,
    "insts": {
        "KILL": {
            "args": 0,
            "description": "End process"
        },
        "OUT": {
            "args": 3,
            "description": "Print arg3 as arg1(type: 0(str), 1(char), 2(dec), 3(list of chars), arg2 is count of bytes (no matter if type=0,1)"
        },
        "SET": {
            "args": 1,
            "description": "Set variable into memory. Arg1 is 1 byte of how many next bytes should be the value"
        },
        "SUM": {
            "args": 3,
            "description": "Sum two numbers and arg1 is destination. Args are addreses" 
        },
        "SUB": {
            "args": 3,
            "description": "Substract two numbers and arg1 is destination. Args are addreses" 
        },
        "CPY": {
            "args": 3,
            "description": "Copy arg1 bytes of arg3 into arg2"
        }
    }
}