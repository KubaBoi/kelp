


class funcs:
    
    @staticmethod
    def prepare_funcs(data_classes: list, key: str) -> dict:
        """
        Prepare list of functions

        Args:
            `data` : dict of tuples "key": [(line index, line content),...]
            
        Return:
            dict of variables and their values
        """